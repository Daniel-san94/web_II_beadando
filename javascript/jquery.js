function velemenyszerkesztes(data)
{
    console.log(data)
    /*const hozzaszolas = $(`#hid-${data.id}`)
     const velemeny = $(`#hid-${data.id} .velemeny`)
    const velemenyertek = velemeny[0].childNodes[0].nodeValue
    console.log(velemenyertek)
     const adat = {id:data.id, velemenyertek:velemenyertek}
    velemeny.html(`<input type="text" name="velemeny" value="${velemeny[0].childNodes[0].nodeValue}">
    <button onclick="">Mentés</button>
    <button onclick="megse(${velemenyertek})">Mégse</button>
    `)*/


}
function megse(data)
{
   /* const velemeny = $(`#hid-${id} .velemeny`)*/
    /*velemeny.text(velemenyertek)*/
    console.log(data)

}